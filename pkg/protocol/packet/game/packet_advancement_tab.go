//go:build ignore
// +build ignore

// Code generated by enhanced-generator v2 from protocol.json; DO NOT EDIT manually.
// To regenerate: go run main_v2.go -protocol <path> -output <dir> -direction server

package server

import (
	"fmt"
	pk "git.konjactw.dev/falloutBot/go-mc/net/packet"
	"git.konjactw.dev/patyhank/minego/pkg/protocol/packetid"
	"io"
)

// AdvancementTab represents the Serverbound AdvancementTab packet.

type AdvancementTab struct {
	Action int32 `mc:"VarInt"`
	// Switch 基於 Action：
	//   0 -> string
	//   1 -> void

	TabId interface{}
}

// PacketID returns the packet ID for this packet.
func (*AdvancementTab) PacketID() packetid.ServerboundPacketID {
	return packetid.ServerboundAdvancementTab
}

// ReadFrom reads the packet data from the reader.
func (p *AdvancementTab) ReadFrom(r io.Reader) (n int64, err error) {
	var temp int64
	_ = temp

	var action pk.VarInt
	temp, err = action.ReadFrom(r)
	n += temp
	if err != nil {
		return n, err
	}
	p.Action = int32(action)

	switch p.Action {
	case 0:
		var val string
		var elem pk.String
		temp, err = elem.ReadFrom(r)
		n += temp
		if err != nil {
			return n, err
		}
		val = string(elem)
		p.TabId = val
	case 1:
		var val interface{}
		p.TabId = val
	default:
		// 無對應負載
	}

	return n, nil
}

// WriteTo writes the packet data to the writer.
func (p AdvancementTab) WriteTo(w io.Writer) (n int64, err error) {
	var temp int64
	_ = temp

	temp, err = pk.VarInt(p.Action).WriteTo(w)
	n += temp
	if err != nil {
		return n, err
	}

	switch v := p.TabId.(type) {
	case string:
		temp, err = pk.String(v).WriteTo(w)
		n += temp
		if err != nil {
			return n, err
		}
	case interface{}:
		if err != nil {
			return n, err
		}
	default:
		return n, fmt.Errorf("unsupported switch type for TabId: %T", v)
	}

	return n, nil
}

func init() {
	registerPacket(packetid.ServerboundAdvancementTab, func() ServerboundPacket {
		return &AdvancementTab{}
	})
}
