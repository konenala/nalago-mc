// Code generated by enhanced-generator v2 from protocol.json; DO NOT EDIT manually.
// To regenerate: go run main_v2.go -protocol <path> -output <dir> -direction client

package client

import (
	"io"

	"git.konjactw.dev/falloutBot/go-mc/data/packetid"
	pk "git.konjactw.dev/falloutBot/go-mc/net/packet"
)

// Login represents the Clientbound Login packet.

type Login struct {
	EntityId            int32
	IsHardcore          bool
	WorldNames          []string
	MaxPlayers          int32 `mc:"VarInt"`
	ViewDistance        int32 `mc:"VarInt"`
	SimulationDistance  int32 `mc:"VarInt"`
	ReducedDebugInfo    bool
	EnableRespawnScreen bool
	DoLimitedCrafting   bool
	WorldState          interface{}
	EnforcesSecureChat  bool
}

// PacketID returns the packet ID for this packet.
func (*Login) PacketID() packetid.ClientboundPacketID {
	return packetid.ClientboundLogin
}

// ReadFrom reads the packet data from the reader.
func (p *Login) ReadFrom(r io.Reader) (n int64, err error) {
	var temp int64

	temp, err = (*pk.Int)(&s.EntityId).ReadFrom(r)
	n += temp
	if err != nil {
		return n, err
	}

	var isHardcore pk.Boolean
	temp, err = isHardcore.ReadFrom(r)
	n += temp
	if err != nil {
		return n, err
	}
	s.IsHardcore = bool(isHardcore)

	var worldNamesCount pk.VarInt
	temp, err = worldNamesCount.ReadFrom(r)
	n += temp
	if err != nil {
		return n, err
	}
	s.WorldNames = make([]string, worldNamesCount)
	for i := 0; i < int(worldNamesCount); i++ {
		var elem pk.String
		temp, err = elem.ReadFrom(r)
		n += temp
		if err != nil {
			return n, err
		}
		p.WorldNames[i] = string(elem)
	}

	var maxPlayers pk.VarInt
	temp, err = maxPlayers.ReadFrom(r)
	n += temp
	if err != nil {
		return n, err
	}
	s.MaxPlayers = int32(maxPlayers)

	var viewDistance pk.VarInt
	temp, err = viewDistance.ReadFrom(r)
	n += temp
	if err != nil {
		return n, err
	}
	s.ViewDistance = int32(viewDistance)

	var simulationDistance pk.VarInt
	temp, err = simulationDistance.ReadFrom(r)
	n += temp
	if err != nil {
		return n, err
	}
	s.SimulationDistance = int32(simulationDistance)

	var reducedDebugInfo pk.Boolean
	temp, err = reducedDebugInfo.ReadFrom(r)
	n += temp
	if err != nil {
		return n, err
	}
	s.ReducedDebugInfo = bool(reducedDebugInfo)

	var enableRespawnScreen pk.Boolean
	temp, err = enableRespawnScreen.ReadFrom(r)
	n += temp
	if err != nil {
		return n, err
	}
	s.EnableRespawnScreen = bool(enableRespawnScreen)

	var doLimitedCrafting pk.Boolean
	temp, err = doLimitedCrafting.ReadFrom(r)
	n += temp
	if err != nil {
		return n, err
	}
	s.DoLimitedCrafting = bool(doLimitedCrafting)

	// TODO: Read WorldState (SpawnInfo)

	var enforcesSecureChat pk.Boolean
	temp, err = enforcesSecureChat.ReadFrom(r)
	n += temp
	if err != nil {
		return n, err
	}
	s.EnforcesSecureChat = bool(enforcesSecureChat)

	return n, nil
}

// WriteTo writes the packet data to the writer.
func (p Login) WriteTo(w io.Writer) (n int64, err error) {
	var temp int64

	temp, err = pk.Int(p.EntityId).WriteTo(w)
	n += temp
	if err != nil {
		return n, err
	}

	temp, err = pk.Boolean(p.IsHardcore).WriteTo(w)
	n += temp
	if err != nil {
		return n, err
	}

	temp, err = pk.VarInt(len(p.WorldNames)).WriteTo(w)
	n += temp
	if err != nil {
		return n, err
	}
	for i := range p.WorldNames {
		temp, err = pk.String(p.WorldNames[i]).WriteTo(w)
		n += temp
		if err != nil {
			return n, err
		}
	}

	temp, err = pk.VarInt(p.MaxPlayers).WriteTo(w)
	n += temp
	if err != nil {
		return n, err
	}

	temp, err = pk.VarInt(p.ViewDistance).WriteTo(w)
	n += temp
	if err != nil {
		return n, err
	}

	temp, err = pk.VarInt(p.SimulationDistance).WriteTo(w)
	n += temp
	if err != nil {
		return n, err
	}

	temp, err = pk.Boolean(p.ReducedDebugInfo).WriteTo(w)
	n += temp
	if err != nil {
		return n, err
	}

	temp, err = pk.Boolean(p.EnableRespawnScreen).WriteTo(w)
	n += temp
	if err != nil {
		return n, err
	}

	temp, err = pk.Boolean(p.DoLimitedCrafting).WriteTo(w)
	n += temp
	if err != nil {
		return n, err
	}

	// TODO: Write WorldState (SpawnInfo)

	temp, err = pk.Boolean(p.EnforcesSecureChat).WriteTo(w)
	n += temp
	if err != nil {
		return n, err
	}

	return n, nil
}

func init() {
	registerPacket(packetid.ClientboundLogin, func() ClientboundPacket {
		return &Login{}
	})
}
