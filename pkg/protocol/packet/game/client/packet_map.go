// Code generated by enhanced-generator v2 from protocol.json; DO NOT EDIT manually.
// To regenerate: go run main_v2.go -protocol <path> -output <dir> -direction client

package client

import (
	"fmt"
	pk "git.konjactw.dev/falloutBot/go-mc/net/packet"
	"git.konjactw.dev/patyhank/minego/pkg/protocol/packetid"
	"io"
)

// Map represents the Clientbound Map packet.

type Map struct {
	ItemDamage int32 `mc:"VarInt"`
	Scale      int8
	Locked     bool
	// TODO: Optional complex type
	Icons   *interface{}
	Columns uint8
	// Switch 基於 Columns：
	//   0 -> void
	//   default -> u8
	Rows interface{}
	// Switch 基於 Columns：
	//   0 -> void
	//   default -> u8
	X interface{}
	// Switch 基於 Columns：
	//   0 -> void
	//   default -> u8
	Y interface{}
	// Switch 基於 Columns：
	//   0 -> void

	Data interface{}
}

// PacketID returns the packet ID for this packet.
func (*Map) PacketID() packetid.ClientboundPacketID {
	return packetid.ClientboundMap
}

// ReadFrom reads the packet data from the reader.
func (p *Map) ReadFrom(r io.Reader) (n int64, err error) {
	var temp int64
	_ = temp

	var itemDamage pk.VarInt
	temp, err = itemDamage.ReadFrom(r)
	n += temp
	if err != nil {
		return n, err
	}
	p.ItemDamage = int32(itemDamage)

	var scale int8
	temp, err = (*pk.Byte)(&scale).ReadFrom(r)
	n += temp
	if err != nil {
		return n, err
	}
	p.Scale = scale

	var locked pk.Boolean
	temp, err = locked.ReadFrom(r)
	n += temp
	if err != nil {
		return n, err
	}
	p.Locked = bool(locked)

	// TODO: Read optional complex type

	var columns pk.UnsignedByte
	temp, err = columns.ReadFrom(r)
	n += temp
	if err != nil {
		return n, err
	}
	p.Columns = uint8(columns)

	switch p.Columns {
	case 0:
		var val interface{}
		p.Rows = val
	default:
		var val uint8
		var elem pk.UnsignedByte
		temp, err = elem.ReadFrom(r)
		n += temp
		if err != nil {
			return n, err
		}
		val = uint8(elem)
		p.Rows = val
	}

	switch p.Columns {
	case 0:
		var val interface{}
		p.X = val
	default:
		var val uint8
		var elem pk.UnsignedByte
		temp, err = elem.ReadFrom(r)
		n += temp
		if err != nil {
			return n, err
		}
		val = uint8(elem)
		p.X = val
	}

	switch p.Columns {
	case 0:
		var val interface{}
		p.Y = val
	default:
		var val uint8
		var elem pk.UnsignedByte
		temp, err = elem.ReadFrom(r)
		n += temp
		if err != nil {
			return n, err
		}
		val = uint8(elem)
		p.Y = val
	}

	switch p.Columns {
	case 0:
		var val interface{}
		p.Data = val
	default:
		// 無對應負載
	}

	return n, nil
}

// WriteTo writes the packet data to the writer.
func (p Map) WriteTo(w io.Writer) (n int64, err error) {
	var temp int64
	_ = temp

	temp, err = pk.VarInt(p.ItemDamage).WriteTo(w)
	n += temp
	if err != nil {
		return n, err
	}

	temp, err = pk.Byte(p.Scale).WriteTo(w)
	n += temp
	if err != nil {
		return n, err
	}

	temp, err = pk.Boolean(p.Locked).WriteTo(w)
	n += temp
	if err != nil {
		return n, err
	}

	// TODO: Write optional complex type

	temp, err = pk.UnsignedByte(p.Columns).WriteTo(w)
	n += temp
	if err != nil {
		return n, err
	}

	switch v := p.Rows.(type) {
	case interface{}:
		if err != nil {
			return n, err
		}
	case uint8:
		temp, err = pk.UnsignedByte(v).WriteTo(w)
		n += temp
		if err != nil {
			return n, err
		}
	default:
		return n, fmt.Errorf("unsupported switch type for Rows: %T", v)
	}

	switch v := p.X.(type) {
	case interface{}:
		if err != nil {
			return n, err
		}
	case uint8:
		temp, err = pk.UnsignedByte(v).WriteTo(w)
		n += temp
		if err != nil {
			return n, err
		}
	default:
		return n, fmt.Errorf("unsupported switch type for X: %T", v)
	}

	switch v := p.Y.(type) {
	case interface{}:
		if err != nil {
			return n, err
		}
	case uint8:
		temp, err = pk.UnsignedByte(v).WriteTo(w)
		n += temp
		if err != nil {
			return n, err
		}
	default:
		return n, fmt.Errorf("unsupported switch type for Y: %T", v)
	}

	switch v := p.Data.(type) {
	case interface{}:
		if err != nil {
			return n, err
		}
	default:
		return n, fmt.Errorf("unsupported switch type for Data: %T", v)
	}

	return n, nil
}

func init() {
	registerPacket(packetid.ClientboundMap, func() ClientboundPacket {
		return &Map{}
	})
}
